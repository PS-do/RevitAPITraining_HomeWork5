<Window x:Class="RevitAPITraining_Task5_1.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RevitAPITraining_Task5_1"
             mc:Ignorable="d" 
        Title="Задание 5.1" Height="129" Width="209">
    <StackPanel>
        <Button Padding="5" Margin="5"
                Content="Количество всех труб в моделе"
                Command="{Binding PipesCountCommand}"/>
        <Button Padding="5" Margin="5"
                Content="Объём всех стен в моделе"
                Command="{Binding VolumeAllWalsCommand}"/>
        <Button Padding="5" Margin="5"
                Content="Количество всех дверей в моделе"
                Command="{Binding DorsCountCommand}"/>
    </StackPanel>
</Window>
